public class Hero : Fighter
{

    // Propriété

    // Ctor
    public Hero(string archetype, string name, int hpMax, int con, int str, int dex, int intel, int mainStat, string damages, int ac) : base(archetype, name, hpMax, con, str, dex, intel, mainStat, damages, ac) { }
}
